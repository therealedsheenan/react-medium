const initial_state = {
  name: 'react-medium'
};

const initReducer = (state = initial_state, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default initReducer;
